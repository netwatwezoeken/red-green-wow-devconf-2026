# Yatzy Python Starter Project

## Prerequisites
- Python >= 3.10

## Getting Started

Run the following:

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e .
pip install pytest
```